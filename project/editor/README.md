# Standalone editor

The standalone editor reads backed-up Palworld saves, applies validated changes to a copy, and preserves the original source. The tested implementation is in this source tree under `pal_editor`; `Run-PalEditor.cmd` launches it for development.
